{
  "enabled": true,
  "name": "Test Reminder",
  "description": "Reminds developers to test their changes using npm run dev after significant modifications",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "src/server/**/*.ts",
      "src/client/**/*.ts"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Significant code changes have been made. If the user hasn't tested recently, gently remind them that they can test their changes by running 'npm run dev' in a separate terminal. This will:\n\n1. Start the Devvit playtest environment\n2. Provide a test URL (e.g., https://www.reddit.com/r/regenx_dev?playtest=regenx)\n3. Allow them to see their changes live in a Reddit post\n\nOnly mention this if it seems like they've made substantial changes and might benefit from testing. Don't be pushy - just a friendly reminder."
  }
}
